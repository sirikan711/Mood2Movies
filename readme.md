# 🎬 Mood2Movie - แนะนำภาพยนตร์ตามอารมณ์ของคุณ

## � Table of Contents (สารบัญ)

- [📋 บทสรุปโปรเจค](#-บทสรุปโปรเจค)
- [🎯 วัตถุประสงค์](#-วัตถุประสงค์ของโปรเจค)
- [✨ ฟีเจอร์หลัก](#-ฟีเจอร์หลัก)
- [🛠️ เทคโนโลยี](#-เทคโนโลยีและเฟรมเวิร์ก)
- [📁 โครงสร้างโปรเจค](#-โครงสร้างโปรเจค)
- [📊 โครงสร้างฐานข้อมูล](#-โครงสร้างฐานข้อมูล-database-models)
- [📊 Mood Scoring Levels](#-ระดับการให้คะแนนอารมณ์-mood-scoring-levels)
- [🧮 IMDb Formula](#-สูตรการคำนวณคะแนน-imdb-weighted-rating-formula)
- [🚀 Installation](#-วิธีการติดตั้งและใช้งาน)
- [🔧 Database Queries](#-database-queries-examples)
- [📌 Application Flow](#-flow-หลักของแอปพลิเคชัน)
- [🎓 Learning Outcomes](#-สิ่งที่เรียนรู้จากโปรเจค)
- [📝 Future Plans](#-หมายเหตุและการพัฒนาในอนาคต)
- [🧪 Testing](#-testing--quality-assurance)
- [🐛 Troubleshooting](#-common-issues--troubleshooting)
- [📚 Resources](#-additional-resources)
- [👨‍💼 Project Info](#-ข้อมูลโปรเจค)
- [📧 Contact](#-ติดต่อและสนับสนุน)

---

## �📋 บทสรุปโปรเจค

**Mood2Movie** เป็นแอปพลิเคชันเว็บที่ช่วยให้ผู้ใช้ค้นหาและค้นพบหนังที่เหมาะสมกับอารมณ์ปัจจุบันของตนเอง ระบบนี้ใช้ Django Framework สร้างแพลตฟอร์มที่ผู้ใช้สามารถรีวิวภาพยนตร์และให้คะแนนระดับความรู้สึก (Mood Score) ได้ จากนั้นระบบจะวิเคราะห์ข้อมูลและแนะนำภาพยนตร์ที่ตรงกับอารมณ์ที่ผู้ใช้ต้องการ

---

## 🎯 วัตถุประสงค์ของโปรเจค

1. **ช่วยผู้ใช้ค้นหาหนังตามอารมณ์** - ให้ผู้ใช้สามารถค้นหาภาพยนตร์โดยอิงตามอารมณ์ที่พวกเขาต้องการ
2. **เก็บข้อมูลรีวิวแบบหลายมิติ** - เก็บรีวิวร่วมกับข้อมูลระดับความเข้มข้นของแต่ละอารมณ์ (Mood Score)
3. **สร้างชุมชนของผู้รักหนัง** - ให้ผู้ใช้สามารถแชร์รีวิว ติดตาม Custom Lists และค้นหาผู้ใช้คนอื่น
4. **จัดการและจัดระเบียบหนัง** - ผู้ใช้สามารถสร้าง Custom Lists, Bookmark, และเก็บรายการโปรดของตนเอง
5. **ตัวอย่างการใช้ API ภายนอก** - ใช้ TMDB (The Movie Database) API เพื่อดึงข้อมูลหนังที่ครอบคลุม

---

## ✨ ฟีเจอร์หลัก

### 🔍 ระบบค้นหาและแนะนำ
- **ค้นหาแบบหลายเกณฑ์** - ค้นหาหนังโดยชื่อ อารมณ์ ปี และประเภท (Genre)
- **แนะนำตามอารมณ์ (Mood-based Recommendation)** - ผู้ใช้เลือกอารมณ์ที่ต้องการ → ระบบค้นหาหนังที่มีคนรีวิวอารมณ์นั้น → จัดอันดับจากมากไปน้อย (โดยอิงจากคะแนนเฉลี่ยและจำนวนรีวิวของอารมณ์ที่เลือก)
- **ฐานข้อมูล Hybrid** - สามารถค้นหาจากฐานข้อมูลท้องถิ่น (Local DB) หรือ TMDB API

### 💬 ระบบรีวิวและความรู้สึก (Mood Scoring)
- **โครงสร้าง Multi-Mood** - ผู้ใช้สามารถให้คะแนนระดับความเข้มข้น (0-5) สำหรับแต่ละอารมณ์ต่อหนังเรื่องเดียว
  - ตัวอย่างอารมณ์: Happy 😊, Sad 😭, Scary 😨, Funny 🤣, Heartwarming 🥰, Tense 😬 เป็นต้น
- **Radar Chart Visualization** - แสดงภาพการกระจายอารมณ์ของหนังแต่ละเรื่องในรูปแบบกราฟ Radar ให้มองเห็นลักษณะเฉพาะของหนัง
- **ความคิดเห็นข้อความ** - ผู้ใช้สามารถเพิ่มความเห็นและเหตุผลในการให้คะแนนอารมณ์
- **ไม่มี Overall Rating** - ระบบไม่ใช้คะแนนรวม (Rating) แต่ใช้ข้อมูลอารมณ์ในการแนะนำแทน

### ❤️ จัดการไลบรารีส่วนตัว
- **Favorites** - เก็บรายการหนังโปรด
- **Bookmarks** - เก็บหนังที่ต้องการดูในภายหลัง
- **Custom Lists** - สร้างรายการหนังที่กำหนดเอง (เช่น "ต้องดูในปีนี้", "หนังแนะนำให้เพื่อน")
  - สามารถตั้งค่าเป็นสาธารณะ (Public) หรือส่วนตัว (Private)

### 👥 ระบบโปรไฟล์และชุมชน
- **โปรไฟล์ผู้ใช้** - แสดงข้อมูลส่วนตัว รูปโปรไฟล์ (Avatar) และสถิติการใช้งาน
- **ค้นหาผู้ใช้** - ค้นหาและเรียกดูโปรไฟล์ผู้ใช้อื่นๆ
- **ดูรีวิวของผู้อื่น** - เรียกดู Custom Lists และรีวิวของผู้ใช้คนอื่น

### 📅 ปฏิทินภาพยนตร์
- **Month View** - แสดงหนังตามวันที่ release
- **Filter by Release Date** - ค้นหาหนังที่ออกฉายในช่วงเวลาที่กำหนด

### 🛡️ ระบบผู้ดูแลระบบ (Admin Dashboard)
- **Custom Admin Interface** - จัดการ Movies, Moods, และ Reviews
- **ลบข้อมูลที่ไม่เหมาะสม** - ผู้ดูแลระบบสามารถลบหนัง รีวิว และอารมณ์ที่ไม่เหมาะสม
- **ตัวอักษรผู้ดูแลระบบ** - เฉพาะสมาชิก Staff เท่านั้นที่สามารถเข้าถึง

---

## 🛠️ เทคโนโลยีและเฟรมเวิร์ก

### Backend
- **Django 5.2.8** - Web Framework สร้างระบบ MTV (Model-Template-View)
- **Python 3.x** - Programming Language
- **PostgreSQL** - Database ที่เก็บ User, Reviews, Movies, Moods, Lists

### Frontend
- **HTML5** - Markup สำหรับสร้าง Template
- **CSS3** - Styling (พร้อม dark theme)
- **JavaScript** - Client-side Interactivity (sliders, dynamic forms)
- **Bootstrap 5** - CSS Framework สำหรับ Responsive Design

### External APIs
- **TMDB API** - ดึงข้อมูลหนัง (search, details, genres, popular movies)

### Libraries & Tools
- **Django ORM** - Database Management และ Query Building
- **psycopg2** - PostgreSQL adapter สำหรับ Python
- **Django Signals** - Automatic Profile Creation (@receiver decorator)
- **Django Forms** - Form Handling (ReviewForm, CustomListForm, SignUpForm)
- **Django Admin** - Custom Admin Interface สำหรับ superusers
- **Django Decorators** - login_required, staff_member_required
- **Python Requests** - HTTP library สำหรับเรียก TMDB API
- **Pillow** - Image processing สำหรับ Avatar upload

---

## 📁 โครงสร้างโปรเจค

```
mood2movie_project/
├── manage.py                    # Django Management Script
├── readme.md                    # Documentation (ไฟล์นี้)
├── db.sqlite3                   # (SQLite - ใช้ PostgreSQL สำหรับ Production)
│
├── config/                      # Project Configuration
│   ├── settings.py              # Django Settings
│   ├── urls.py                  # URL Routing (Main)
│   ├── asgi.py                  # ASGI Configuration
│   ├── wsgi.py                  # WSGI Configuration
│   └── __pycache__/
│
├── core/                        # Core App
│   ├── models.py                # Models
│   ├── views.py                 # Views (Home page)
│   ├── urls.py                  # URL Routing
│   ├── admin.py                 # Admin Configuration
│   ├── forms.py                 # Forms (if any)
│   ├── tests.py                 # Unit Tests
│   ├── migrations/              # Database Migrations
│   └── __pycache__/
│
├── movies/                      # Main Movies App
│   ├── models.py                # Movie Models
│   │   ├── Movie                # Film Information
│   │   ├── Mood                 # Emotion Categories
│   │   ├── Review               # User Reviews
│   │   ├── ReviewMoodScore      # Mood Intensity Scores (Multi-dimensional)
│   │   ├── Favorite             # Bookmarked Films
│   │   ├── Bookmark             # Films to Watch Later
│   │   ├── CustomList           # User-created Lists
│   │   └── Profile              # User Profiles
│   ├── views.py                 # Views (Detailed Logic - 678 lines)
│   ├── urls.py                  # URL Routing
│   ├── forms.py                 # Forms (Review, CustomList)
│   ├── admin.py                 # Admin Configuration
│   ├── utils.py                 # Utility Functions (TMDB API, helpers)
│   ├── tests.py                 # Unit Tests
│   ├── migrations/              # Database Migrations
│   ├── templatetags/            # Custom Template Tags
│   │   ├── custom_filters.py    # Custom Template Filters
│   │   └── __pycache__/
│   └── __pycache__/
│
├── accounts/                    # User Accounts App
│   ├── models.py                # (Empty - User model from Django Auth)
│   ├── views.py                 # Views (Register, Login, Profile Edit)
│   ├── urls.py                  # URL Routing
│   ├── forms.py                 # Forms (Registration, Profile)
│   ├── admin.py                 # Admin Configuration
│   ├── tests.py                 # Unit Tests
│   ├── migrations/              # Database Migrations
│   └── __pycache__/
│
├── templates/                   # HTML Templates
│   ├── base.html                # Base Template (Navigation, Layout)
│   ├── accounts/
│   │   ├── login.html           # Login Page
│   │   ├── signup.html          # Registration Page
│   │   ├── profile.html         # User Profile View
│   │   └── edit_profile.html    # Profile Edit Form
│   ├── core/
│   │   └── home.html            # Home Page
│   ├── movies/
│   │   ├── search.html          # Search & Filter Interface
│   │   ├── detail.html          # Movie Detail & Reviews
│   │   ├── add_review.html      # Add Review Form
│   │   ├── edit_review.html     # Edit Review Form
│   │   ├── recommendation.html  # Mood-based Recommendations
│   │   ├── calendar.html        # Movie Release Calendar
│   │   ├── search_users.html    # User Search Results
│   │   ├── lists/
│   │   │   ├── my_lists.html    # User's Custom Lists
│   │   │   ├── create_list.html # Create New List
│   │   │   ├── edit_list.html   # Edit List Details
│   │   │   ├── list_detail.html # List Detail View
│   │   │   └── user_lists.html  # View Other Users' Lists
│   │   └── admin/
│   │       ├── dashboard.html   # Admin Dashboard
│   │       ├── movies.html      # Manage Movies
│   │       ├── moods.html       # Manage Moods
│   │       └── reviews.html     # Manage Reviews
│   └── registration/
│       └── login.html           # Django Auth Login
│
├── media/                       # User Uploaded Files
│   └── avatars/                 # Profile Pictures
│
└── static/                      # Static Files (CSS, JS, Images) - if needed
    ├── css/
    ├── js/
    └── images/
```

---

## 📊 โครงสร้างฐานข้อมูล (Database Models)

### Relationships Diagram

```
┌─────────────┐
│    User     │ (Django Auth)
└──────┬──────┘
       │
       ├─── (1:1) ──→ Profile
       │
       ├─── (1:N) ──→ Review
       │
       ├─── (1:N) ──→ Favorite
       │
       ├─── (1:N) ──→ Bookmark
       │
       └─── (1:N) ──→ CustomList
                         │
                         └─── (N:M) ──→ Movie
                                 │
                                 └─── (1:N) ──→ Review
                                         │
                                         └─── (1:N) ──→ ReviewMoodScore
                                                 │
                                                 └─── (N:1) ──→ Mood
```

### หลัก (Core Models)

#### **Movie**
- `tmdb_id` (Integer, Unique) - ตัวระบุจาก TMDB
- `title` (CharField, max 255) - ชื่อหนัง
- `poster_path` (CharField, nullable) - URL โปสเตอร์
- `overview` (TextField, nullable) - คำอธิบายหนัง
- `release_date` (DateField, nullable) - วันที่ออกฉาย
- `vote_average` (FloatField, default 0.0) - คะแนนเฉลี่ยจาก TMDB (ไม่ใช้สำหรับการแนะนำ)

#### **Mood**
- `name` (CharField, Unique) - ชื่ออารมณ์ (เช่น "Happy", "Sad")
- `emoji` (Property) - ไอคอนอารมณ์ (😊, 😭, เป็นต้น)

#### **Review**
- `movie` (ForeignKey → Movie) - ลิงก์ไปยังหนัง
- `user` (ForeignKey → User) - ผู้เขียนรีวิว
- `comment` (TextField) - ความเห็น/ความคิดเห็น
- `created_at` (DateTimeField) - วันที่สร้าง

#### **ReviewMoodScore**
- `review` (ForeignKey → Review) - ลิงก์ไปยังรีวิว
- `mood` (ForeignKey → Mood) - อารมณ์ที่ให้คะแนน
- `intensity` (IntegerField, 0-5) - ระดับความเข้มข้น (ดู [ระดับการให้คะแนนอารมณ์](#-ระดับการให้คะแนนอารมณ์-mood-scoring-levels) สำหรับรายละเอียด)
- `Meta.unique_together` - ไม่สามารถให้คะแนนอารมณ์เดียวกันสองครั้งต่อรีวิว

### ปฏิสัมพันธ์ผู้ใช้ (User Interactions)

#### **Favorite**
- `user` (ForeignKey → User) - ผู้ใช้
- `movie` (ForeignKey → Movie) - หนังโปรด
- `created_at` (DateTimeField) - วันที่เพิ่มโปรด

#### **Bookmark**
- `user` (ForeignKey → User) - ผู้ใช้
- `movie` (ForeignKey → Movie) - หนังที่ต้องการดู
- `created_at` (DateTimeField) - วันที่เพิ่ม

#### **CustomList**
- `user` (ForeignKey → User) - เจ้าของรายการ
- `name` (CharField) - ชื่อรายการ
- `description` (TextField) - คำอธิบาย
- `movies` (ManyToManyField → Movie) - หนังในรายการ
- `is_public` (BooleanField) - สาธารณะหรือส่วนตัว
- `created_at` (DateTimeField) - วันที่สร้าง

#### **Profile**
- `user` (OneToOneField → User) - ลิงก์ผู้ใช้ (Django Signal สร้างอัตโนมัติ)
- `bio` (TextField, max 500 chars) - ชีวประวัติ (optional)
- `avatar` (ImageField) - รูปโปรไฟล์ (เก็บใน `media/avatars/`)

---

## � ระดับการให้คะแนนอารมณ์ (Mood Scoring Levels)

ในระบบ Mood2Movie ผู้ใช้สามารถให้คะแนนความเข้มข้นของแต่ละอารมณ์ได้ 6 ระดับ (0-5) ดังนี้:

| ระดับ | ค่า | คำอธิบาย | ตัวอย่าง |
|------|-----|---------|---------|
| **ไม่ให้คะแนน** | 0 | ไม่มีอารมณ์นี้ในหนัง | หนังที่ไม่เศร้า |
| **เบาบาง** | 1 | มีเพียงเล็กน้อย มีฉากไม่กี่ฉาก | มีฉากที่น่าเศร้าเพียงจุดเดียว |
| **ปานกลาง** | 2 | มีสัดส่วนปานกลาง | หนังมีบางส่วนที่เศร้า |
| **ค่อนข้างเข้มข้น** | 3 | สาระสำคัญของหนัง | หนังที่เศร้าสลับสบายใจ |
| **เข้มข้นมาก** | 4 | อารมณ์นี้เป็นธีมหลัก | หนังที่เศร้าเป็นส่วนใหญ่ |
| **เข้มข้นมากที่สุด** | 5 | อารมณ์นี้ครอบงำทั้งเรื่อง | หนังเศร้าจากต้นจนจบ |

### ตัวอย่างการให้คะแนน

**ตัวอย่างหนัง: "The Shawshank Redemption" (คำอธิบายเชิงมนุษย์)**
```
Happy     (😊): 2  - มีบางฉากแสดงความหวัง
Sad       (😭): 3  - มีเนื้อหาเศร้า (คุก กักตัว)
Inspiring (🥰): 4  - เป็นธีมหลัก การกระทำที่ใหญ่หลวง
Tense     (😬): 3  - มีความตึงเครียดไม่ต่อเนื่อง
Funny     (🤣): 1  - มีจุกเล็กน้อย
Scary     (😨): 0  - ไม่มีองค์ประกอบสยองขวัญ
```

**ตัวอย่างหนัง: "Avengers: Endgame"**
```
Happy     (😊): 2  - มีบางช่วงที่สนุก
Action    (⚡): 5  - เป็นหนังแอคชั่นแท้ (ครอบงำทั้งเรื่อง)
Sad       (😭): 3  - มีความเสียใจ (ผลที่ตามมา)
Inspiring (🥰): 4  - ธีมเกี่ยวกับเสียสละ
Tense     (😬): 4  - ความตึงเครียดสูง
Scary     (😨): 1  - อาจจะน้อยหน่อย
```

### ประโยชน์ของ Mood Scoring

1. **ค้นหาแบบแม่นยำ** - ผู้ใช้สามารถค้นหาหนังที่มีอารมณ์เฉพาะที่มีความเข้มข้นในระดับที่ต้องการ
2. **Radar Chart Visualization** - แสดงรูปแบบอารมณ์ของหนังในรูปกราฟ Radar เพื่อให้มองเห็นได้ชัดเจน
3. **Mood-based Recommendation** - ระบบแนะนำหนังจากความเป็นจริง (crowd-sourced) ตามอารมณ์ที่เลือก และจัดอันดับจากน้อยไปมาก
4. **ไม่มี Overall Rating Bias** - ไม่ใช้ Overall Rating ที่อาจเอนเอียง แต่ใช้ Mood-specific Data ที่รวบรวมจากรีวิวผู้ใช้

### วิธีการหาแนะนำหนัง (Recommendation Flow)

เมื่อผู้ใช้เลือกอารมณ์หนึ่งเพื่อหาแนะนำ ระบบจะ:

1. **ค้นหารีวิวที่มีอารมณ์นั้น** → Query Reviews ที่มี Mood Score > 0 สำหรับอารมณ์ที่เลือก
2. **คำนวณค่าเฉลี่ย** → หาคะแนนเฉลี่ยของอารมณ์นั้นต่อหนังแต่ละเรื่อง
3. **ใช้ Weighted Formula** → ปรับคะแนนด้วยสูตร Bayesian Estimate เพื่อป้องกันอคติ
4. **จัดอันดับจากมากไปน้อย** → เรียงลำดับหนังตามคะแนนอารมณ์สูงสุด
5. **แสดงผลลัพธ์** → แสดงหนังพร้อม Radar Chart, จำนวนรีวิว, และคะแนนเฉลี่ยของอารมณ์

---

## 🧮 สูตรการคำนวณคะแนน (IMDb Weighted Rating Formula - Adapted for Mood)

แม้ว่าโปรเจค Mood2Movie ยังไม่ได้นำสูตร IMDb Formula มาใช้ในการคำนวณคะแนนรวม (Overall Rating) แต่ได้นำแนวคิด Bayesian Estimation มาปรับใช้สำหรับการคำนวณคะแนนอารมณ์เฉพาะด้าน

### Mood-Specific Weighted Score Formula

$$\text{WM} = \left( \frac{v}{v+m} \right) \times R + \left( \frac{m}{v+m} \right) \times C$$

โดยที่:
- **WM** = Weighted Mood Score (คะแนนอารมณ์ที่ปรับปรุงแล้ว) (0-5)
- **v** = จำนวนรีวิวที่มีอารมณ์นี้ (intensity > 0) สำหรับหนังนั้นๆ
- **m** = ค่าต่ำสุดของจำนวนรีวิว (threshold = 1, ค่าคงที่)
- **R** = คะแนนเฉลี่ยของอารมณ์นั้นสำหรับหนังนั้นๆ (0-5)
- **C** = คะแนนเฉลี่ยของอารมณ์นั้นในระบบทั้งหมด (global average)

### ตัวแปรในสูตร

| ตัวแปร | ความหมาย | หน่วย |
|------|---------|------|
| **WM** | Weighted Mood Score | คะแนนอารมณ์ที่ปรับปรุงแล้ว (0-5) |
| **v_m** | จำนวนรีวิวสำหรับอารมณ์นี้ | จำนวน (reviews) |
| **m** | Minimum Reviews Threshold | จำนวน (เช่น 5, 10) |
| **R_m** | Mean Mood Score | ค่าเฉลี่ย (0-5) |
| **C_m** | Community Average | ค่าเฉลี่ยของชุมชน (0-5) |

### วิธีการคำนวณ

**ขั้นตอนที่ 1:** หาค่าน้ำหนัก (Weight) สำหรับอารมณ์เฉพาะ
- น้ำหนักของรีวิวจริง = $\frac{v_m}{v_m+m}$
- น้ำหนักของค่าเฉลี่ยชุมชน = $\frac{m}{v_m+m}$

**ขั้นตอนที่ 2:** คำนวณคะแนนอารมณ์ที่ปรับปรุงแล้ว
- WM = (น้ำหนักของรีวิวจริง × R_m) + (น้ำหนักของค่าเฉลี่ยชุมชน × C_m)

### ตัวอย่างการคำนวณ

**สมมติฐาน:**
- หนัง "Interstellar" มี 8 รีวิวที่เลือก "Inspiring" mood
- คะแนน Inspiring ของหนัง: [5, 4, 5, 3, 4, 5, 4, 5] = เฉลี่ย R = 4.375
- m = 1 (ค่าคงที่)
- คะแนนเฉลี่ย "Inspiring" ของทั้งระบบ C = 3.8

**การคำนวณ:**

$$\text{WM} = \left( \frac{8}{8+1} \right) \times 4.375 + \left( \frac{1}{8+1} \right) \times 3.8$$

$$\text{WM} = \left( \frac{8}{9} \right) \times 4.375 + \left( \frac{1}{9} \right) \times 3.8$$

$$\text{WM} = 0.889 \times 4.375 + 0.111 \times 3.8$$

$$\text{WM} = 3.889 + 0.422 = 4.31$$

**ผลลัพธ์:** หนัง "Interstellar" ได้คะแนน Inspiring ที่ปรับปรุงแล้ว **4.31/5.0**

### เหตุผลการใช้ Bayesian Estimate

1. **ป้องกันอคติ (Bias)** - หนังที่มี 1 รีวิว กับ 5/5 คะแนน Inspiring จะไม่ได้เปรียบหนังที่มี 50 รีวิว
2. **ยุติธรรมมากขึ้น** - รวมความเห็นของชุมชนโดยรวมเข้าด้วย ไม่เอนเอียงไปทางหนังใหม่
3. **คะแนนมีเสถียรภาพ** - ยิ่งมีรีวิวมากขึ้น คะแนนจึงยิ่งแสดงความเป็นจริงมากขึ้น

### การประยุกต์ใช้ใน Mood2Movie

ระบบแนะนำ (Recommendation) ปัจจุบันใช้แนวคิดนี้เพื่อ:
- ✅ คำนวณคะแนนอารมณ์ที่เชื่อถือได้ สำหรับแต่ละหนัง
- ✅ จัดอันดับหนังจากน้อยไปมาก โดยพิจารณาจำนวนรีวิวและคะแนน
- ✅ ป้องกันหนังที่มีรีวิวน้อย จากการติดอันดับสูง
- ✅ แสดงเฉพาะหนังที่มีความเชื่อถือได้ (มีจำนวนรีวิวพอสมควร) ในหน้าแนะนำ

---

## 🚀 วิธีการติดตั้งและใช้งาน

### 1. ติดตั้ง Requirements
```bash
# ติดตั้ง Python packages ที่จำเป็น
pip install django==5.2.8
pip install psycopg2-binary      # PostgreSQL adapter
pip install requests             # สำหรับการเรียก TMDB API
pip install pillow               # สำหรับการจัดการรูปภาพ (Avatar)
pip install python-dotenv        # (Optional) สำหรับการเก็บ API Keys
```

**หมายเหตุ:** ถ้าใช้ `requirements.txt` ให้รัน:
```bash
pip install -r requirements.txt
```

### 2. ตั้งค่า PostgreSQL Database

#### 2.1 สร้าง Database
```bash
# เข้าสู่ PostgreSQL prompt
psql -U postgres

# สร้าง Database ใหม่
CREATE DATABASE mood2movie;

# สร้าง User (Optional)
CREATE USER mood2user WITH PASSWORD 'your_password';

# ให้ permissions ให้ user
ALTER ROLE mood2user SET client_encoding TO 'utf8';
ALTER ROLE mood2user SET default_transaction_isolation TO 'read committed';
ALTER ROLE mood2user SET default_transaction_deferrable TO on;
ALTER ROLE mood2user SET default_transaction_read_uncommitted TO off;
GRANT ALL PRIVILEGES ON DATABASE mood2movie TO mood2user;
```

#### 2.2 ตั้งค่า Django Settings
แก้ไข `config/settings.py`:

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'mood2movie',
        'USER': 'mood2user',           # หรือ 'postgres' ถ้าใช้ superuser
        'PASSWORD': 'your_password',
        'HOST': 'localhost',           # หรือ '127.0.0.1'
        'PORT': '5432',                # Default PostgreSQL port
    }
}
```

หรือใช้ Environment Variables:
```bash
# .env
DB_ENGINE=django.db.backends.postgresql
DB_NAME=mood2movie
DB_USER=mood2user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

```python
# config/settings.py
import os
from dotenv import load_dotenv

load_dotenv()

DATABASES = {
    'default': {
        'ENGINE': os.getenv('DB_ENGINE'),
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST'),
        'PORT': os.getenv('DB_PORT'),
    }
}
```

### 3. สร้างฐานข้อมูล
```bash
# สร้าง Migration Files
python manage.py makemigrations

# Apply Migrations ลงฐานข้อมูล
python manage.py migrate
```

### 4. สร้าง Superuser (Admin)
```bash
python manage.py createsuperuser
```

คำถาม:
- **Username:** ชื่อผู้ดูแล (เช่น `admin`)
- **Email:** อีเมลของผู้ดูแล
- **Password:** รหัสผ่าน (ป้อน 2 ครั้ง)

### 5. โหลดข้อมูล Initial (Fixture)
ถ้าโปรเจคมี Fixtures สำหรับ Moods ให้รัน:
```bash
python manage.py loaddata fixtures/moods.json  # (ถ้ามี)
```

ถ้าไม่มี สามารถสร้างอารมณ์ (Moods) ผ่าน Admin Panel หลังจากรันเซิร์ฟเวอร์

### 6. รันเซิร์ฟเวอร์
```bash
# รันเซิร์ฟเวอร์ Development
python manage.py runserver

# หรือกำหนด Port เฉพาะ (default: 8000)
python manage.py runserver 8001
```

### 7. เข้าถึงแอปพลิเคชัน

| URL | ลักษณะ | หมายเหตุ |
|-----|--------|---------|
| `http://127.0.0.1:8000/` | Home Page | หน้าแรก |
| `http://127.0.0.1:8000/admin/` | Admin Panel | สำหรับผู้ดูแล (Superuser) |
| `http://127.0.0.1:8000/accounts/signup/` | Register | สมัครสมาชิกใหม่ |
| `http://127.0.0.1:8000/accounts/login/` | Login | เข้าสู่ระบบ |
| `http://127.0.0.1:8000/movies/search/` | Search | ค้นหาหนัง |

### 8. Seeding Initial Data (อพเดทข้อมูล)

สามารถเพิ่มข้อมูลเริ่มต้นผ่าน Admin Panel:

**1. เพิ่มอารมณ์ (Moods)**
- ไปที่ `/admin/movies/mood/`
- กด **"Add Mood"**
- เพิ่มอารมณ์เช่น: Happy, Sad, Scary, Funny, Heartwarming, Tense, etc.

**2. เพิ่มหนัง (Movies) - Optional**
- ไปที่ `/admin/movies/movie/`
- กด **"Add Movie"**
- ดึงข้อมูลจาก TMDB API ผ่านฟังก์ชัน (หรือเพิ่มด้วยตนเอง)

### 8. Configuration (ปรับแต่งการตั้งค่า)

#### ตั้งค่า TMDB API Key

1. สมัครสมาชิกที่ [TMDB](https://www.themoviedb.org/settings/api)
2. ได้รับ API Key
3. เพิ่ม API Key ลงใน `movies/utils.py`:

```python
# movies/utils.py
TMDB_API_KEY = "YOUR_API_KEY_HERE"
TMDB_BASE_URL = "https://api.themoviedb.org/3"
```

หรือใช้ Environment Variable:
```bash
# .env
TMDB_API_KEY=YOUR_API_KEY_HERE
```

```python
# settings.py
import os
from dotenv import load_dotenv

load_dotenv()
TMDB_API_KEY = os.getenv('TMDB_API_KEY')
```

#### ตั้งค่า Media Files

แน่ใจว่า `MEDIA_ROOT` และ `MEDIA_URL` ตั้งค่าถูกต้องใน `config/settings.py`:

```python
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

### 9. Production Deployment (ปรับใช้บนเซิร์ฟเวอร์)

#### ตั้งค่า TMDB API Key

1. สมัครสมาชิกที่ [TMDB](https://www.themoviedb.org/settings/api)
2. ได้รับ API Key
3. เพิ่ม API Key ลงใน `movies/utils.py`:

```python
# movies/utils.py
TMDB_API_KEY = "YOUR_API_KEY_HERE"
TMDB_BASE_URL = "https://api.themoviedb.org/3"
```

หรือใช้ Environment Variable:
```bash
# .env
TMDB_API_KEY=YOUR_API_KEY_HERE
```

```python
# settings.py
import os
from dotenv import load_dotenv

load_dotenv()
TMDB_API_KEY = os.getenv('TMDB_API_KEY')
```

#### ตั้งค่า Media Files

แน่ใจว่า `MEDIA_ROOT` และ `MEDIA_URL` ตั้งค่าถูกต้องใน `config/settings.py`:

```python
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

### 9. Production Deployment (ปรับใช้บนเซิร์ฟเวอร์)

สำหรับการปรับใช้บนเซิร์ฟเวอร์จริง:

1. **Collect Static Files**
```bash
python manage.py collectstatic --no-input
```

2. **ปรับปรุง Settings**
```python
# config/settings.py
DEBUG = False
ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']
SECRET_KEY = os.getenv('SECRET_KEY')  # ย้ายไปใน .env

# PostgreSQL Connection
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST'),
        'PORT': os.getenv('DB_PORT'),
    }
}
```

3. **ใช้ WSGI Server** เช่น Gunicorn:
```bash
pip install gunicorn
gunicorn config.wsgi:application --bind 0.0.0.0:8000
```

4. **ใช้ Web Server** เช่น Nginx หรือ Apache

---

---

#### ตั้งค่า TMDB API Key

1. สมัครสมาชิกที่ [TMDB](https://www.themoviedb.org/settings/api)
2. ได้รับ API Key
3. เพิ่ม API Key ลงใน `movies/utils.py` หรือ `.env` file

```python
# ตัวอย่าง (ใน movies/utils.py)
TMDB_API_KEY = "YOUR_API_KEY_HERE"
TMDB_BASE_URL = "https://api.themoviedb.org/3"
```

### API Functions ที่ใช้

- `search_movies_tmdb(query, year=None, genre_id=None)` - ค้นหาหนังจาก TMDB
- `get_movie_details_tmdb(tmdb_id)` - ดึงรายละเอียดหนังเรื่องเดียว
- `get_tmdb_genres()` - ดึงรายชนิดหนัง (genres)
- `get_popular_movies_tmdb()` - ดึงหนังยอดนิยม (สำหรับ home page)
- `get_movies_in_date_range(start_date, end_date)` - ค้นหาหนังตามช่วงวันที่ (สำหรับ calendar)

---

## � API Response Examples

### TMDB Search API Response

```json
{
  "results": [
    {
      "id": 550,
      "title": "Fight Club",
      "poster_path": "/rFarDISfrBRj7dUY0MSQ3fvM1dA.jpg",
      "overview": "A ticking-time-bomb...",
      "release_date": "1999-10-15",
      "vote_average": 8.8
    }
  ]
}
```

### ReviewMoodScore Response (JSON)

```json
{
  "review_id": 1,
  "movie_id": 550,
  "movie_title": "Fight Club",
  "user": "john_doe",
  "comment": "Outstanding movie!",
  "mood_scores": [
    {
      "mood": "Happy",
      "intensity": 1,
      "emoji": "😊"
    },
    {
      "mood": "Tense",
      "intensity": 5,
      "emoji": "😬"
    },
    {
      "mood": "Thought-provoking",
      "intensity": 5,
      "emoji": "🤔"
    }
  ],
  "created_at": "2024-01-30T12:34:56Z"
}
```

---

## 🔧 Database Queries Examples

### ค้นหาหนังตามอารมณ์

```python
from movies.models import Movie, ReviewMoodScore, Mood

# ค้นหาหนังที่มีคะแนน "Happy" มากกว่า 3
mood = Mood.objects.get(name='Happy')
movies = Movie.objects.filter(
    reviews__mood_scores__mood=mood,
    reviews__mood_scores__intensity__gte=3
).distinct()

# หรือใช้ Aggregation เพื่อหาคะแนนเฉลี่ย
from django.db.models import Avg

movies_with_avg = Movie.objects.annotate(
    avg_mood_score=Avg('reviews__mood_scores__intensity')
).filter(
    reviews__mood_scores__mood=mood,
    avg_mood_score__gte=3
).distinct()
```

### ดึงรีวิวพร้อม Mood Scores

```python
from movies.models import Review

# ดึงรีวิวพร้อมข้อมูล Mood Scores
reviews = Review.objects.select_related(
    'user', 'movie'
).prefetch_related(
    'mood_scores__mood'
).filter(
    movie__title__icontains='Avatar'
)

# วนลูปเพื่อแสดงข้อมูล
for review in reviews:
    print(f"{review.user.username}: {review.comment}")
    for score in review.mood_scores.all():
        print(f"  - {score.mood.name}: {score.intensity}")
```

### สร้าง Custom List

```python
from movies.models import CustomList, Movie

# สร้างรายการใหม่
custom_list = CustomList.objects.create(
    user=request.user,
    name="Must Watch in 2024",
    description="My favorite movies this year",
    is_public=True
)

# เพิ่มหนังลงในรายการ
movie1 = Movie.objects.get(tmdb_id=550)
movie2 = Movie.objects.get(tmdb_id=278)
custom_list.movies.add(movie1, movie2)

# ถอดหนังออกจากรายการ
custom_list.movies.remove(movie1)
```

---

## 📌 Flow หลักของแอปพลิเคชัน
```
User → Sign Up → Create Account → Auto Create Profile → Login
```

### 2. **Search & Discovery**
```
User → Search Movies (by title/mood/year/genre)
     ↓
     → Search Local DB (if mood selected)
     → OR Search TMDB API (if no mood)
     → Display Results
```

### 3. **Movie Review & Rating**
```
User → View Movie Detail → Add Review
     → Select Mood Scores (0-5 for each mood)
     → Add Comment
     → Submit
     → Review Saved with ReviewMoodScore records
```

### 4. **Mood Recommendation (แนะนำตามอารมณ์)**
```
User → Select Mood (เลือกอารมณ์ที่ต้องการ)
    ↓
System → Query Reviews with Selected Mood
       → Calculate Average Mood Intensity for That Mood
       → Filter Movies by Review Count (ต้องมีรีวิวพอสมควร)
       → Rank Movies from Highest to Lowest Mood Score
       → Display with Radar Chart & Review Count
    ↓
Results → Show Top Movies with That Mood
        → Show Average Mood Intensity Score
        → Show Number of Reviews
```

### 5. **Library Management**
```
User → Create Custom List
     → Add/Remove Movies
     → Set Public/Private
     → Share with Others
```

---

## 🎓 สิ่งที่เรียนรู้จากโปรเจค

### Django Concepts
- ✅ Models & ORM (Foreign Keys, Many-to-Many, One-to-One)
- ✅ Model Validators (MinValueValidator, MaxValueValidator) 
- ✅ Django Signals (Auto Profile Creation on User Registration using @receiver)
- ✅ Views & URL Routing (Path Converters, Pattern matching)
- ✅ Forms & Validation (Custom Forms with ModelForm, UserCreationForm)
- ✅ Authentication & Permissions (login_required, staff_member_required decorators)
- ✅ Admin Customization (Custom Admin Views)
- ✅ Template Rendering & Context passing
- ✅ Query Optimization (select_related, prefetch_related, Filter with Q objects, Count, Avg)
- ✅ Unique Together Constraints (ไม่ให้เลือกอารมณ์เดียวกัน 2 ครั้งต่อรีวิว)
- ✅ Related Names & Reverse Relations (user.reviews.all(), movie.reviews.all())

### Web Development Concepts
- ✅ RESTful API Integration (TMDB API - search, get details, genres)
- ✅ Database Design & Relationships (Complex ER with ReviewMoodScore)
- ✅ User-Generated Content Management (Reviews, Custom Lists)
- ✅ Multi-criteria Search & Advanced Filtering (by title, mood, year, genre)
- ✅ Responsive UI/UX Design (with Bootstrap, dark theme)
- ✅ Data Visualization (Radar Charts สำหรับ Mood Distribution)
- ✅ Form Handling & Submission (File upload for avatar, dynamic form fields)
- ✅ User Authorization Checks (Duplicate review prevention, owner checks)

### Python Skills
- ✅ Working with External APIs (requests library, JSON parsing)
- ✅ DateTime Handling (date, timedelta, DateTimeField, auto_now_add)
- ✅ List & Dictionary Comprehensions
- ✅ Exception Handling (try-except, get_object_or_404)
- ✅ Lambda Functions and Decorators
- ✅ Decorators (@receiver, @login_required, @staff_member_required)
- ✅ String Formatting (f-strings)
- ✅ File operations (avatar upload handling)

### Data Analysis & Algorithms
- ✅ Statistical Calculations (Average, Aggregation with Avg(), Count())
- ✅ Bayesian Estimation (Weighted Mood Score Formula)
- ✅ Data Normalization & Scaling (0-5 intensity scale)
- ✅ Weighted Scoring Systems (combining user data with global data)
- ✅ Ranking & Sorting Algorithms (order_by('-mood_score'))

---

## 📝 หมายเหตุและการพัฒนาในอนาคต

### Current Status
- ✅ ค้นหาหนังแบบหลายเกณฑ์
- ✅ ระบบรีวิวแบบ Multi-Mood พร้อม 6 ระดับความเข้มข้น (0-5)
- ✅ ระบบแนะนำตามอารมณ์ (Mood-based Ranking) - จัดอันดับจากน้อยไปมากตามคะแนนอารมณ์
- ✅ Radar Chart Visualization สำหรับแต่ละหนัง
- ✅ Custom Lists
- ✅ Admin Dashboard
- ✅ ปฏิทินภาพยนตร์
- ✅ ระบบบันทึก Avatar (รูปโปรไฟล์)
- ✅ Django Signals สำหรับสร้างโปรไฟล์อัตโนมัติ
- ✅ Weighted Mood Score Calculation (Bayesian Estimate)

### Future Enhancements (ปรับปรุงในอนาคต)
- 🔄 Implement Full Weighted Mood Score Ranking ให้ครบถ้วนในทุก queries
- 🔄 Machine Learning Recommendation Engine (Collaborative Filtering, Content-based)
- 🔄 Real-time Notifications (แจ้งเตือนเมื่อมีรีวิวใหม่)
- 🔄 Social Features (Follow users, Like reviews, Comments on reviews)
- 🔄 Advanced Analytics Dashboard (สถิติการดูหนัง, Popular moods)
- 🔄 Export Data (ส่งออก Custom Lists, Reviews as PDF)
- 🔄 Mobile App Version (iOS/Android)
- 🔄 Dockerization for Deployment (Docker Compose setup)
- 🔄 API Documentation (Swagger/OpenAPI)
- 🔄 Performance Optimization (Caching, Celery for async tasks)

---

### Database
```
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'mood2movie_db',      # ชื่อ DB ที่สร้าง
        'USER': 'postgres',           # หรือ username ที่สร้าง
        'PASSWORD': '****',           # รหัสผ่าน
        'HOST': 'localhost',          # หรือ IP address
        'PORT': '5432',               # Port มาตรฐาน
    }
}
```

### Unit Tests

```bash
# รันทั้งหมด Tests
python manage.py test

# รันเฉพาะ App
python manage.py test movies
python manage.py test accounts

# รับ Verbose Output
python manage.py test --verbosity=2

# Coverage Test
pip install coverage
coverage run --source='.' manage.py test
coverage report
coverage html
```

### Testing Example

```python
# movies/tests.py
from django.test import TestCase
from django.contrib.auth.models import User
from movies.models import Movie, Review, Mood, ReviewMoodScore

class ReviewTestCase(TestCase):
    def setUp(self):
        self.user = User.objects.create_user(
            username='testuser',
            password='testpass123'
        )
        self.movie = Movie.objects.create(
            tmdb_id=550,
            title='Fight Club'
        )
        self.mood = Mood.objects.create(name='Tense')

    def test_create_review(self):
        """ทดสอบการสร้างรีวิว"""
        review = Review.objects.create(
            user=self.user,
            movie=self.movie,
            comment="Great movie!"
        )
        self.assertEqual(review.user.username, 'testuser')
        self.assertEqual(review.movie.title, 'Fight Club')

    def test_mood_score(self):
        """ทดสอบการให้คะแนนอารมณ์"""
        review = Review.objects.create(
            user=self.user,
            movie=self.movie,
            comment="Intense!"
        )
        score = ReviewMoodScore.objects.create(
            review=review,
            mood=self.mood,
            intensity=5
        )
        self.assertEqual(score.intensity, 5)
```

---

## 🐛 Common Issues & Troubleshooting

### Issue 1: TMDB API Key ไม่ถูกต้อง
**Error:** `KeyError: 'results'`

**Solution:**
- ตรวจสอบ API Key ใน `movies/utils.py`
- ตรวจสอบ Internet Connection
- ลองใช้ [TMDB API Tester](https://www.themoviedb.org/settings/api) เพื่อยืนยัน API Key

### Issue 2: Migration Error
**Error:** `django.db.utils.OperationalError: no such table: movies_movie`

**Solution:**
```bash
python manage.py makemigrations
python manage.py migrate
```

### Issue 3: Avatar Upload ล้มเหลว
**Error:** `FileNotFoundError: [Errno 2] No such file or directory: 'media/avatars/'`

**Solution:**
```bash
# สร้าง media directory
mkdir -p media/avatars

# ตรวจสอบ MEDIA_ROOT ใน settings.py
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
MEDIA_URL = '/media/'
```

### Issue 4: Static Files ไม่โหลด
**Solution:**
```bash
python manage.py collectstatic --noinput
```

### Issue 5: Import Error
**Error:** `ModuleNotFoundError: No module named 'requests'`

**Solution:**
```bash
pip install requests
```

---

## 📚 Additional Resources

### Documentation Links
- [Django Official Documentation](https://docs.djangoproject.com/)
- [TMDB API Documentation](https://developer.themoviedb.org/docs)
- [Django Models ORM](https://docs.djangoproject.com/en/5.2/topics/db/models/)
- [Django Forms](https://docs.djangoproject.com/en/5.2/topics/forms/)

### Recommended Tutorials
- [Django for Beginners](https://djangoforbeginners.com/)
- [Real Python Django Tutorials](https://realpython.com/tutorials/django/)
- [Database Design Fundamentals](https://www.coursera.org/learn/database-design-web-apps)
- [PostgreSQL with Django](https://www.digitalocean.com/community/tutorials/how-to-use-postgresql-with-your-django-application)

### Tools & Extensions
- **VS Code Extensions:**
  - Python
  - Django
  - PostgreSQL
- **Browser Extensions:**
  - JSON Viewer (สำหรับดู API responses)
  - REST Client (สำหรับ Test API)
- **Database Tools:**
  - pgAdmin (PostgreSQL GUI)
  - DBeaver (Database management)

---

### Settings Configuration

โปรเจค Mood2Movie ใช้Settings ดังนี้:

```python
# Language & Timezone
LANGUAGE_CODE = 'th'          # ภาษาไทย
TIME_ZONE = 'Asia/Bangkok'    # เวลา Thailand

# Authentication URLs
LOGIN_REDIRECT_URL = 'home'
LOGOUT_REDIRECT_URL = 'home'

# Default Auto Field
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
```

### Installed Apps

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    
    # Custom Apps
    'core',         # หน้าแรก
    'movies',       # ระบบหนัง (Search, Reviews, Lists)
    'accounts',     # ระบบผู้ใช้ (Auth, Profile)
]
```

### Middleware

```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]
```

### Custom Template Tags

ระบบมี Custom Template Tags และ Filters สำหรับการใช้งานใน Templates:

```python
# movies/templatetags/custom_filters.py
@register.filter
def get_item(dictionary, key):
    """ดึงค่าจาก Dictionary โดยใช้ Key ใน Template
    
    ตัวอย่าง:
    {{ movie_dict|get_item:"title" }}
    """
    return dictionary.get(key)

@register.filter
def pad_zero(value):
    """เติมเลข 0 ข้างหน้าถ้าเป็นเลขหลักเดียว
    
    ตัวอย่าง:
    {{ 5|pad_zero }}  → 05
    {{ 30|pad_zero }} → 30
    """
    return str(value).zfill(2)
```

#### การใช้งาน Template Tags

```html
<!-- ใน templates -->
{% load custom_filters %}

<!-- ใช้ get_item filter -->
<p>{{ movie_data|get_item:"title" }}</p>

<!-- ใช้ pad_zero filter -->
<p>{{ month|pad_zero }}/{{ day|pad_zero }}/{{ year }}</p>
```

### Security Settings

ส่วนการตั้งค่าความปลอดภัย ปัจจุบันตั้งค่าสำหรับ Development:

```python
# config/settings.py

# ⚠️ SECURITY WARNING: ไม่ต้องเปิด DEBUG ใน Production!
DEBUG = True

# Secret Key (ต้องเก็บเป็นความลับ)
SECRET_KEY = 'django-insecure-...'  # ⚠️ ต้องเปลี่ยนสำหรับ Production

# Allowed Hosts (สำหรับ Production ต้องเปลี่ยนเป็นโดเมนจริง)
ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# CSRF Token (ปกป้องจาก CSRF attacks)
CSRF_TRUSTED_ORIGINS = []  # เพิ่ม trusted origins สำหรับ Production

# Security Headers (แนะนำสำหรับ Production)
# SECURE_SSL_REDIRECT = True
# SESSION_COOKIE_SECURE = True
# CSRF_COOKIE_SECURE = True
# SECURE_HSTS_SECONDS = 31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS = True
# X_FRAME_OPTIONS = 'DENY'
```

#### Security Checklist สำหรับ Production

```bash
# 1. เพิ่มตรวจสอบ Security
python manage.py check --deploy

# 2. เก็บ Secret Key ใน Environment Variables
export DJANGO_SECRET_KEY='your-secret-key'

# 3. เปลี่ยน DEBUG = False
DEBUG = False

# 4. อัปเดต ALLOWED_HOSTS
ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']

# 5. Enable HTTPS/SSL
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True

# 6. Set HSTS Header (HTTP Strict Transport Security)
SECURE_HSTS_SECONDS = 31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True

# 7. Set X-Frame-Options
X_FRAME_OPTIONS = 'DENY'

# 8. Database Credentials - ใช้ Environment Variables
DB_PASSWORD = os.getenv('DB_PASSWORD')
TMDB_API_KEY = os.getenv('TMDB_API_KEY')
```

### Requirements & Dependencies

โปรเจค Mood2Movie ต้องการ Python packages ดังต่อไปนี้:

#### Core Dependencies (จำเป็น)

| Package | Version | ลักษณะ |
|---------|---------|--------|
| **Django** | 5.2.8 | Web Framework |
| **psycopg2-binary** | 2.9.9 | PostgreSQL Database Adapter |
| **requests** | 2.31.0 | HTTP Library สำหรับ TMDB API |
| **Pillow** | 10.1.0 | Image Processing (Avatar) |
| **python-dotenv** | 1.0.0 | Environment Variables Management |

#### Optional Dependencies

| Package | Version | ลักษณะ |
|---------|---------|--------|
| **gunicorn** | 21.2.0 | WSGI Server (Production) |
| **django-extensions** | 3.2.3 | Django Development Tools |
| **django-debug-toolbar** | 4.2.0 | Debugging Tools (Development) |
| **celery** | 5.3.4 | Async Task Queue |
| **redis** | 5.0.1 | Caching & Message Broker |
| **pytest** | 7.4.3 | Testing Framework |
| **pytest-django** | 4.7.0 | Django Testing Plugin |
| **black** | 23.12.0 | Code Formatter |
| **flake8** | 6.1.0 | Linter |

#### การติดตั้ง

**ตัวเลือก 1: ใช้ requirements.txt (แนะนำ)**
```bash
pip install -r requirements.txt
```

**ตัวเลือก 2: ติดตั้งแต่ละตัว**
```bash
# Core only
pip install Django==5.2.8 psycopg2-binary==2.9.9 requests==2.31.0 Pillow==10.1.0 python-dotenv==1.0.0

# + Production Server
pip install gunicorn==21.2.0

# + Development Tools
pip install django-extensions==3.2.3 django-debug-toolbar==4.2.0

# + Testing
pip install pytest==7.4.3 pytest-django==4.7.0

# + Code Quality
pip install black==23.12.0 flake8==6.1.0 pylint==3.0.3
```

#### Checking Installed Packages

```bash
# ตรวจสอบ Python version
python --version  # ต้อง 3.8+

# ตรวจสอบเวอร์ชัน Django
python -c "import django; print(django.get_version())"

# ตรวจสอบ installed packages
pip list

# ตรวจสอบ PostgreSQL connection
python manage.py dbshell
```

---

## ✅ Verification Summary (สรุปการตรวจสอบ)

README ได้ตรวจสอบแล้วกับโปรเจคจริง ❌ มีจุดที่ต้องปรับให้สอดคล้อง:

| ข้อมูล | README | โปรเจคจริง | สถานะ |
|------|--------|---------|--------|
| **Models** | 8 ✅ | 8 ✅ | ✅ ตรง |
| **URL Routes** | 20+ | 25+ | ✅ ปรับแล้ว |
| **View Functions** | 15+ | 25+ | ✅ ปรับแล้ว |
| **Mood Scoring** | 0-5 ✅ | 0-5 ✅ | ✅ ตรง |
| **Templates** | 20+ ✅ | 20+ ✅ | ✅ ตรง |
| **Forms** | ReviewForm, CustomListForm ✅ | ✅ มี SignUpForm เพิ่มเติม | ✅ ปรับแล้ว |
| **API Functions** | 4 | 5 ✅ (มี get_popular_movies_tmdb) | ✅ ปรับแล้ว |
| **Weighted Formula** | m = 1 ✅ | m = 1 ✅ | ✅ ตรง |
| **Profile Fields** | bio, avatar ✅ | ✅ (no created_at) | ✅ ปรับแล้ว |
| **Movie Fields** | 6 fields ✅ | 6 fields ✅ | ✅ ตรง |

---

- **ชื่อโปรเจค:** Mood2Movie
- **สาขา:** DSSI (Digital Software and Systems Integration)
- **ชั้นปี:** 3
- **ประเภท:** Django Web Application with PostgreSQL
- **ภาษาหลัก:** Python, HTML, CSS, JavaScript
- **เฟรมเวิร์ก:** Django 5.2.8
- **ฐานข้อมูล:** PostgreSQL
- **API ภายนอก:** TMDB (The Movie Database)

---

## 📊 Project Statistics

| Metric | Value |
|--------|-------|
| **Total Models** | 8 (Movie, Mood, Review, ReviewMoodScore, Favorite, Bookmark, CustomList, Profile) |
| **Database Tables** | 8 |
| **URLs Routes** | 25+ |
| **Views Functions** | 25+ (including search, recommendations, admin, list management) |
| **Templates** | 20+ HTML files |
| **Lines of Code (views.py)** | 678 |
| **Features** | 8 Major Features + 5 Future Enhancements |
| **Supported Moods** | 7+ (Happy, Sad, Scary, Funny, Heartwarming, Tense, etc.) |

---

## 📧 ติดต่อและสนับสนุน

หากมีคำถามหรือต้องการแก้ไข สามารถติดต่อผู้พัฒนา หรือสร้าง Issue ได้ที่ Repository นี้

**Repository:** [sirikan711/Mood2Movies](https://github.com/sirikan711/Mood2Movies)

---

## 📄 License

โปรเจคนี้ใช้สิทธิ์แบบ **MIT License** - สามารถใช้งานและแก้ไขได้อย่างอิสระ

---

## 🙏 Acknowledgments

- [The Movie Database (TMDB)](https://www.themoviedb.org/) - สำหรับ Movie Data
- [Django Community](https://www.djangoproject.com/) - สำหรับ Framework
- [Bootstrap](https://getbootstrap.com/) - สำหรับ UI Framework
- ครูและอาจารย์ที่สนับสนุนโปรเจค

---

**Last Updated:** January 30, 2026  
**Version:** 1.0.0  
**Status:** ✅ Active Development
